# Development and testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pytest-timeout>=2.4.0
httpx>=0.27.0
# Exact pin, deliberately: a floor lets a contributor's older ruff enforce a
# different rule set than CI (removed rules, format drift). Pin == the version CI
# installs so local and CI enforce identical rules and `format --check` agrees.
ruff==0.15.15

# Documentation lives in a separate repo (kainpl/docs.bamdude.top) and is built
# by that repo's own CI, so its deps are not pinned here — nothing in this
# project imports mkdocs.
#
# To preview or validate the docs locally, install them into THIS venv:
#
#     pip install -r ../docs.bamdude.top/requirements.txt
#     cd ../docs.bamdude.top && mkdocs build --strict
#
# (The docs repo has no venv of its own, and mkdocs shares no dependency with
# the backend, so it lands here without disturbing anything.) `--strict` is what
# the docs CI runs, and since 2026-08-16 it fails on a missing anchor too.

# Security scanning
bandit[sarif]>=1.7.0
pip-audit>=2.7.0
# Transitive of pip-audit -> CacheControl. 1.2.1 patches GHSA-6v7p-g79w-8964.
msgpack>=1.2.1

# Git hook runner. .pre-commit-config.yaml is committed and the hook is
# installed in every working copy, so without this a freshly built venv cannot
# commit at all — `git commit` dies with "No module named pre_commit". It was
# only ever present because older venvs had it installed by hand.
pre-commit>=4.0.0
