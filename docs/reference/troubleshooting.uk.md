---
title: Усунення неполадок
description: Поширені проблеми та їх вирішення
---

# Усунення неполадок

Рішення для поширених проблем з BamDude.

---

## :material-printer-3d: Проблеми з підключенням принтера

### Принтер не підключається

**Симптоми:** Принтер показується як відключений, червоний індикатор.

**Рішення:**

1. **Переконайтесь, що Developer Mode увімкнений**
   - Settings > Network > LAN Only Mode (ON)
   - Потім увімкніть Developer Mode
   - Вимкніть/увімкніть для отримання нового коду доступу

2. **Перевірте IP-адресу**
   - Перевірте IP у мережевих налаштуваннях принтера
   - Використовуйте статичну IP або резервацію DHCP

3. **Перевірте код доступу**
   - Код доступу змінюється при перемиканні Developer Mode
   - Скопіюйте код точно (з урахуванням регістру)

4. **Перевірте мережеве з'єднання**
   ```bash
   ping YOUR_PRINTER_IP
   ```

5. **Переконайтесь, що порти доступні**
   - MQTT: порт 8883
   - FTPS: порт 990

6. **Перевірте правила фаєрвола**

---

### З'єднання часто обривається

1. **Перевірте рівень WiFi сигналу** на картці принтера
2. **Перевантаження мережі** -- Спробуйте виділену мережу/VLAN
3. **Проблеми з роутером** -- Перезавантажте, перевірте прошивку, вимкніть "розумні" функції
4. **Перевірте логи BamDude:**
   ```bash
   tail -f logs/bamdude.log
   ```

---

## :material-camera: Проблеми з камерою

### Потік не запускається

1. Принтер увімкнений?
2. Камера увімкнена в налаштуваннях принтера?
3. ffmpeg встановлений? (включений у Docker-образ)
4. Developer Mode увімкнений?
5. Користувачі Docker: спробуйте `network_mode: host`

### Потік зависає

- Перевірте рівень WiFi сигналу
- Спробуйте зменшити FPS
- Використовуйте режим знімків замість потоку

---

## :material-archive: Проблеми з архівуванням

### Друки не архівуються

1. **SD-карта встановлена?** Потрібна для завантаження файлів
2. **Developer Mode увімкнений?** Потрібен для доступу через FTP
3. **Автоархівування увімкнено?** Перевірте налаштування принтера
4. **Калібрувальні друки** автоматично пропускаються

---

## :material-clock-outline: Проблеми з чергою

### Друки не починаються

1. **Принтер підключений?** Повинен показувати зелений індикатор
2. **Стіл очищений?** Перевірте, чи показується кнопка "Очистити стіл та почати наступний"
3. **Заплановано на майбутнє?** Перевірте, чи друк має запланований час у майбутньому
4. **Режим Queue Only?** Перевірте наявність фіолетового бейджа "Staged"

---

## :material-docker: Проблеми з Docker

### Контейнер не запускається

```bash
docker compose logs bamdude
```

### Не вдається підключитися до принтера

```bash
docker compose exec bamdude ping YOUR_PRINTER_IP
```

Спробуйте `network_mode: host` на Linux.

### macOS / Windows Docker

Docker Desktop запускає контейнери у віртуальній машині. Використовуйте прокидання портів замість host mode та додавайте принтери вручну за IP.

---

## :material-send: Проблеми з Telegram-ботом

### Бот не відповідає

1. Перевірте, що провайдер Telegram увімкнений у Налаштування > Сповіщення
2. Переконайтесь, що токен бота правильний
3. Перевірте логи BamDude на помилки polling
4. Переконайтесь, що ваш чат авторизований

### Команди не працюють

1. Перевірте, що ваш чат має необхідні дозволи
2. Перевірте призначення групи чату у веб-інтерфейсі
3. Спробуйте `/start` для повторної реєстрації чату

---

## :material-database: Проблеми з базою даних

### Скидання бази даних

!!! danger "Втрата даних"
    Це видалить всю вашу історію друку та налаштування!

```bash
docker compose down
# Видаліть файл бази даних з data volume
docker compose up -d
```

---

## :material-bug: Отримання допомоги

При створенні звіту про проблему вкажіть:

- Версію BamDude
- Модель принтера та версію прошивки
- Операційну систему
- Кроки для відтворення
- Повідомлення про помилки з логів
- Конфігурацію Docker Compose (якщо застосовно)

Створюйте Issues на [github.com/kainpl/bamdude/issues](https://github.com/kainpl/bamdude/issues).

> Початково базується на документації [Bambuddy](https://github.com/maziggy/bambuddy).
