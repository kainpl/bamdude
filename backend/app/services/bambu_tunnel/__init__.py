"""Client for the Bambu file tunnel (TCP 6000) — the channel that reaches a
printer's internal storage, which FTP cannot see.

Protocol reference: vault ``60-specs/bambu-file-tunnel-protocol.md``.
"""
