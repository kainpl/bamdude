"""Golden vectors for the forecast engine — frozen from the SHIPPED rate math.

``generate_vectors.py`` is the only writer of ``rate_vectors.json``; the pair is
committed together so the provenance of every expected number is executable.
"""
