# Web Framework
# Pinned <0.136.0 (mirrors upstream Bambuddy v0.2.4.4): fastapi 0.136.x declares
# an undocumented optional `fastar` (Rust-tar) dep in its [standard] extra,
# flagged as a supply-chain / namespace-abuse vector (MAL-2026-4750, Amazon
# Inspector / OSV malicious-packages feed). We install plain `fastapi` (no
# [standard] extra — uvicorn[standard] is unrelated), so `fastar` is never
# pulled in practice, but holding to 0.135.x — which has every feature we use,
# incl. SSE — keeps the flagged version out of the tree entirely and lets the
# pip-audit MAL-2026-4750 suppression be dropped. Lift the cap once upstream
# removes the dep or the advisory clears.
fastapi>=0.109.0,<0.136.0
uvicorn[standard]>=0.27.0

# Database
# Floor is 2.0.38, not 2.0.0: core/database._create_engine() passes pool_size/
# max_overflow on the SQLite branch, which 2.0.38 first accepts (aiosqlite file
# DBs switched NullPool -> AsyncAdaptedQueuePool). Older releases raise TypeError
# at import and take down every SQLite install + the whole test suite.
sqlalchemy>=2.0.38
# Upper bound comes from zigpy, which pins aiosqlite<0.22.0. Pip resolves it
# correctly from a clean install, but `pip install -U aiosqlite` on a dev machine
# will pull 0.22.x and quietly break the Zigbee stack — so the cap is written
# here too, where anyone touching this line will see it.
aiosqlite>=0.19.0,<0.22.0
asyncpg>=0.29.0  # PostgreSQL async driver (optional — install only if using PostgreSQL)
greenlet>=3.0.0

# Pydantic
pydantic>=2.0.0
pydantic-settings>=2.14.2  # GHSA-4xgf-cpjx-pc3j (secrets-dir loader; precautionary)
# Transitive of pydantic-settings, floor-pinned to patch CVE-2026-28684 (dotenv 1.2.1)
python-dotenv>=1.2.2

# Bambu Lab Printer Communication
paho-mqtt>=2.0.0
aioftp>=0.22.0

# Virtual Printer (emulates Bambu printer for slicer uploads)
#
# There is deliberately no FTP server library here. The VP's implicit-FTPS
# server is hand-rolled on asyncio (services/virtual_printer/ftp_server.py).
# pyftpdlib sat under this heading for a long time, but nothing outside the FTP
# *test* mock ever imported it — so a test-only dependency was being installed
# into the Docker image and the Windows installer. That mock is stdlib-only as
# of 2026-07-27 and the dependency is gone; don't re-add one without checking
# that first-party code actually imports it.
cryptography>=48.0.1  # GHSA-537c-gmf6-5ccf (supersedes the CVE-2026-34073 floor)

# 3MF Processing (standard zipfile is sufficient for Bambu 3MF files)
defusedxml>=0.7.0  # Safe XML parsing (prevents XXE attacks)

# Excel Export
openpyxl>=3.1.0

# Notifications
pywebpush>=2.0.0

# Utilities
python-multipart>=0.0.31  # CVE-2026-53538/53539/53540 (supersedes CVE-2026-42561 floor on 0.0.27)
aiofiles>=23.0.0

# QR Code generation
qrcode[pil]>=7.4.0
# 1D barcodes on labels. Pure Python (no C extension, no Ghostscript, no
# cairo), MIT, and it rasterises through Pillow which is already here.
# reportlab carries 20 symbologies too, but only as vector — its raster
# backend needs rlPyCairo, and a cairo binding is real weight on the armv7l
# images this project builds.
python-barcode[images]>=0.15.0

# PDF spool labels (B.1 from upstream 0.2.4b2)
reportlab>=4.0.0

# STL Thumbnail Generation + calibration mesh editing. The calibration
# tower modes (Temp / Retraction / VFA / Max-Volumetric-Speed) Z-cut the
# scaffold mesh down to the requested sweep range: trimesh ``mesh.split``
# separates connected bodies, then ``slice_plane(cap=True)`` cuts each and
# triangulates a watertight cap. Those paths pull in trimesh's otherwise
# optional heavy deps — a graph engine (scipy / networkx) for split +
# section, and polygon triangulation (shapely + mapbox-earcut) for the cap.
# Without them the towers fail with "no graph engines available" /
# "ModuleNotFoundError: scipy". (The PA Tower still scales via the 3MF
# build-item transform; only the range-trimmed towers need the cut.)
trimesh>=4.0.0
scipy>=1.11.0
networkx>=3.0
shapely>=2.0.0
mapbox-earcut>=1.0.0
matplotlib>=3.8.0
fast-simplification>=0.1.0
# trimesh's 3MF loader imports lxml lazily inside the 3MF code path (model.xml
# parse). It isn't a strict trimesh transitive, but the loader raises
# ModuleNotFoundError without it. Needed by services/plate_thumbnail.py, which
# reloads the sliced 3MF's own mesh to render missing plate_N.png thumbnails
# server-side (BS/Orca sidecar CLIs skip them in headless --export-3mf).
lxml>=5.0

# System monitoring
psutil>=6.0.0

# Authentication
# >=2.13.0: picks up the four advisories pip-audit flags against 2.12.x. Our
# usage is unaffected by 2.13.0's behavioural changes — the HS256 secret is a
# generated 64-byte token_urlsafe (well over the enforced 32-byte HMAC floor)
# and the OIDC decode path uses the raw signing key, not the PyJWK wrapper
# (upstream Bambuddy v0.2.4.5 / c36c8cea).
PyJWT>=2.13.0
passlib[bcrypt]>=1.7.4
ldap3>=2.9.0
# TOTP (RFC 6238) for 2FA authenticator apps (§18.1)
pyotp>=2.9.0

# Transitive pin: idna<3.15 has CVE-2026-45409 (ReDoS on encode() with a
# crafted Unicode label). Pulled in by anyio/httpx/requests/yarl; pin the
# floor directly so we don't regress when a downstream loosens its constraint.
idna>=3.15

# Transitive pin: urllib3 2.6.3 has CVE-2026-44431 and CVE-2026-44432;
# 2.7.0+ is the fixed release. Direct pin here because none of our
# top-level deps require >=2.7.0 yet, so without this the resolver
# would silently keep installing the vulnerable 2.6.x line.
urllib3>=2.7.0

# Transitive of fastapi: floor-pinned to 1.3.1 for CVE-2026-54282/54283 (supersedes
# the earlier 1.0.1 floor for PYSEC-2026-161). fastapi's <0.136.0 range still admits
# a build that resolves against starlette 1.3.1 (upstream Bambuddy v0.2.4.8 ships the
# identical fastapi cap + this floor).
starlette>=1.3.1

# Transitive of pywebpush (unpinned `aiohttp` requirement): aiohttp 3.13.5 has
# CVE-2026-34993 and CVE-2026-47265, both fixed in 3.14.0. Floor-pin here so
# the resolver stops installing the vulnerable build. Our direct use in
# services/external_camera.py (ClientSession / ClientTimeout / ClientError /
# iter_chunked) is unchanged in 3.14.x.
aiohttp>=3.14.0

# Plate Detection (optional - enables build plate empty detection)
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Development
pytest>=9.0.3  # CVE-2025-71176 (tmp_path traversal)
pytest-asyncio>=0.23.0
httpx>=0.26.0
# Chrome/Firefox TLS impersonation — needed for bambulab.com firmware check
# because Cloudflare blocks plain httpx via TLS fingerprinting.
curl_cffi>=0.15.0
ruff>=0.2.0

pillow>=12.2.0  # CVE-2026-40192 (JPEG decoding heap overflow)
aiogram>=3.20

# IANA tz database for Windows. The stdlib ``zoneinfo`` module reads the
# system tz database on Linux/macOS, but Windows has none — and the embedded
# Python our Windows installer ships doesn't carry one either, so even
# ``ZoneInfo("UTC")`` raises ``ZoneInfoNotFoundError``. ``local_backup._local_zone``
# catches that and falls back to stdlib UTC, so nothing 500s — but a Windows user
# who sets TZ to a real zone silently gets their scheduled backup at the wrong
# wall-clock hour. ``tzdata`` is the official PyPI package that fills the gap.
tzdata>=2024.1; sys_platform == "win32"

# Zigbee coordinator (own radio — no Home Assistant, no Zigbee2MQTT sidecar).
# Unconditional rather than an optional extra on purpose: an extra doubles the
# install matrix and breaks first on the Windows installer, which builds its own
# environment — the place a missing optional dependency is hardest to diagnose.
# Twelve light pure-Python packages. bellows speaks EZSP to Silabs radios
# (EFR32MG24 in the SONOFF Dongle-M); zigpy is the stack above it.
zigpy>=2.1.0
bellows>=1.0.0

# Per-device workarounds, the same set Home Assistant's ZHA loads. Not optional
# polish: the plug this was developed against ships a firmware that keeps
# reporting the last measured power, current and voltage after the socket is
# switched off, so BamDude read 33 W from a socket with nothing running. The
# quirk for it forces those readings to zero on switch-off and blocks further
# updates until the socket is on again. Every cheap plug has some version of
# this, and guessing at them one model at a time is not a plan.
#
# Pulls ``zha`` (the quirk-authoring API imports its enums) plus zigpy's other
# radio libraries — deconz, znp, xbee, zigate, ziggurat. About 10 MB of pure
# Python all told, and those radio libraries are what a second supported dongle
# would need anyway.
zha-quirks>=2.2.0
