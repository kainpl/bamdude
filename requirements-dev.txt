# Development and testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pytest-timeout>=2.4.0
httpx>=0.27.0
ruff>=0.8.0

# Required by pyftpdlib TLS_FTPHandler for mock FTP server tests
pyOpenSSL>=26.3.0  # avoids capping cryptography <47 in the test env

# Documentation lives in a separate repo (kainpl/docs.bamdude.top) — no
# mkdocs build deps here. Install them in that repo's venv if you need to
# preview docs locally.

# Security scanning
bandit[sarif]>=1.7.0
pip-audit>=2.7.0
# Transitive of pip-audit -> CacheControl. 1.2.1 patches GHSA-6v7p-g79w-8964.
msgpack>=1.2.1
